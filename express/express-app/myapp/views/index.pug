extends layout

block content
 h1= title
 p Welcome to #{title}
 #messages
 #form
  form(onsubmit='return addMessage(this)')
   input#username(type='text', placeholder='Tu Nombre')
   input#texto(type='text', placeholder='Cuéntanos algo...')
   input(type='submit', value='Enviar!')
